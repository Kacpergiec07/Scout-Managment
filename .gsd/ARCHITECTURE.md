# Architecture

> Auto-generated by /map on 2026-04-14

## Overview

A modern Next.js 16 application using the App Router, Shadcn UI components, and Tailwind CSS 4 for styling. The project is currently in its initial stage with a simple entry point and basic theme support.

```
┌─────────────────────────────────────────┐
│              [app/page.tsx]             │
├─────────────────────────────────────────┤
│         [Next.js App Router]            │
├─────────────────────────────────────────┤
│            [UI Components]              │
└─────────────────────────────────────────┘
```

## Components

### Page (Root)
- **Purpose:** Entry point for the application.
- **Location:** `app/page.tsx`
- **Dependencies:** `components/ui/button.tsx`

### UI Components (Shadcn)
- **Purpose:** Reusable UI elements.
- **Location:** `components/ui/`
- **Dependencies:** `lucide-react`, `class-variance-authority`

### Theme Provider
- **Purpose:** Handles dark mode and theme switching.
- **Location:** `components/theme-provider.tsx`
- **Dependencies:** `next-themes`

## Data Flow

1. **Request:** User visits the root URL.
2. **Layout:** `app/layout.tsx` wraps the page, applying global styles and theme provider.
3. **Render:** `app/page.tsx` renders the initial view with a "Project ready!" message.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Next.js | Framework | Core application framework |
| Tailwind CSS | CSS | Styling and theme management |

## Technical Debt

- [ ] **Minimal Content:** Currently just a placeholder page.
- [ ] **Project Structure:** Root directory contains non-app files (adapters, scripts) that might belong elsewhere.

## Conventions

**Naming:** PascalCase for components, kebab-case for directories/utilities.
**Structure:** Next.js App Router (`app/`), shared components in `components/`, utilities in `lib/`.
**Testing:** No testing framework detected yet.
